import { PlaceholderPage } from "@/components/placeholder-page";

export default function InsightsPage() {
  return (
    <PlaceholderPage
      description="当前阶段只收集原始记录。记录数量足够后，这里会成为分析层的页面位置。"
      title="Insights"
    />
  );
}
